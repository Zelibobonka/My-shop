import styled from "styled-components";

const RadioLabel = styled.label`
  display: flex;
  align-items: center;
  gap: 5px;
  font-weight: 500;
  cursor: pointer;

  & span {
    font-size: 1rem;
  }
`;
export default RadioLabel;
